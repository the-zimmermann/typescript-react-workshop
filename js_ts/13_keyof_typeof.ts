type Cow = {
  name: string;
  age: number;
};

const monica: Cow = {
  name: "monica",
  age: 2,
};

/**
 * @todo Write a handler function to functionally update the cows properties and autocompletion for the keys
 */
